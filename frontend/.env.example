# cp .env.example .env  — Metro: npm run start -- --reset-cache
API_URL=https://www.proparcel.com
MODELS_URL=https://www.proparcel.com

# Mapbox public token (harita — pk.*). Mapbox hesabi: account.mapbox.com/access-tokens
EXPO_PUBLIC_MAPBOX_TOKEN=pk.your_public_token_here

# Android SDK indirme token (build icin — sk.*). android/gradle.properties MAPBOX_DOWNLOADS_TOKEN
# RNMAPBOX_MAPS_DOWNLOAD_TOKEN=sk.your_downloads_token_here

# Yerel Django (adb reverse tcp:8000 tcp:8000):
# API_URL=http://127.0.0.1:8000
# MODELS_URL=http://127.0.0.1:8000
